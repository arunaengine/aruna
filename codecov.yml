coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0.8%
    patch:
      default:
        target: 80%
        # Large feature merges cannot land 80% diff coverage in one pass;
        # the patch status reports without blocking, the project status guards
        # against overall regressions.
        informational: true
