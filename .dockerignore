# Build context exclusions for the node image.
# Copyright (c) 2026 The Aruna Contributors
# SPDX-License-Identifier: MIT or Apache-2.0

.git
target
**/.env
# The startup guard embeds the tracked demonstration profile with include_str!,
# so the build needs it. Only binaries reach the final stage, so it never ships.
!.env
.compose-data
docker/
